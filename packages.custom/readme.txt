这里保存的是自己修改过的，nuget上不能下载的程序集

dotliquid
	修改：
		允许include和extend标签的模板缓存
		允许显示完整的例外信息
		改进GetSafeTypeTransformer的性能和支持通过泛型接口获取
		修复在多线程下使用同一Template对象的问题
		引入FastReflection大幅度改进性能
	https://github.com/303248153/dotliquid
